import SearchBar from "./components/SearchBar";
import apiKey from "../config.js";

function App() {
  const fetchPics = (genre) => {};

  return (
    <div className="App">
      <SearchBar />
    </div>
  );
}

export default App;
